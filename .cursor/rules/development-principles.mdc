---
description: 开发与实现准则 — 新需求与改 bug 均适用；设计原则、数据结构、最小 diff、保留历史注释
alwaysApply: true
---

# 开发与实现准则

**新需求、新功能与修复/重构同样适用。** 按以下**优先级**决策（前者优先于后者）：

## 1. 软件六大设计原则

- **单一职责**：一个类型/模块只承担一类变化原因
- **开闭**：对扩展开放，对修改关闭；新能力优先扩展点，少改稳定核心
- **里氏替换**：子类型可替换父类型而不破坏行为契约
- **接口隔离**：接口小而专，不强迫实现无用方法
- **依赖倒置**：依赖抽象（协议/接口），不依赖具体实现
- **迪米特法则**：少跨层知晓细节，只与直接协作对象通信

## 2. 数据结构优先

职责边界清晰后，再选/调整数据结构（模型、枚举、配置聚合等），让行为自然、分支变少。

## 3. 最优方案下，最小 diff

在满足 1、2 的前提下，选最优方案，但改动范围尽量小：不重构无关代码，不顺便「整理」未触及的文件。新功能也避免一次性铺过大表面。

## 4. 综合设计 — 优雅、简洁、清晰、高效

- 命名、分层、文件组织与现有代码一致
- 类型内用 `// MARK: -` 按职责分区；协议 `extension` 只放协议方法（见 `swift-file-structure.mdc`）
- 控制流可读，避免过度抽象与一行 helper
- **新增**注释只解释「为什么」，不写显而易见的「做了什么」

## 5. 注释与历史上下文（硬约束）

**编辑已有文件时，默认保留全部现有注释。** 重构、搬移、重命名代码不等于删除注释。

- **禁止**为「代码更短 / 更干净」删除仍有效的注释（含文件头说明、`///` 步骤注释、边界/协议/固件对照、inline 设计说明）
- 代码块移动或改写时，**注释随逻辑一并迁移**；语义变化则**改注释**，不要删了了事
- 仅当注释与代码**已矛盾且无法更新**、或内容**完全重复/错误**时才删除，并在回复/commit 中说明删了哪条、为什么
- 允许在保留原注释基础上**补充**新说明（如 R2 supplementary）；不要用新注释替换整段旧注释除非旧注释已过时
- 改完后自检：对比 diff，若净删注释行数 > 0，须逐条说明理由或恢复
